<div id="footer">
    <div class="container">
        <p class="pull-right text-muted">{{ Carbon\Carbon::now()->toFormattedDateString() }}</p>
        <p class="text-muted credit">© LaraBase, {{ Carbon\Carbon::now()->year }}</p>
    </div>
</div>